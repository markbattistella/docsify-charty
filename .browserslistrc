# Browsers we support.
#
# Charty leans on CSS features that all became broadly available during 2023
# and 2024 — :has(), light-dark(), oklch(), nesting, and container queries —
# so there is no version here that predates them. Older browsers get the
# markup and the data, just without the styling.
defaults
not dead
